{
  "name": "bookbridge-full-stack",
  "version": "1.0.0",
  "description": "BookBridge - Student Book Exchange & Donation Platform",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ..",
    "start": "npm run start-backend & npm run start-frontend",
    "start-backend": "cd backend && npm run dev",
    "start-frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "prod-build": "npm run build-backend && npm run build-frontend",
    "build-backend": "cd backend && npm run build",
    "build-frontend": "cd frontend && npm run build"
  },
  "author": "BookBridge Team",
  "license": "MIT",
  "dependencies": {
    "react": "^19.2.6",
    "react-icons": "^5.6.0",
    "react-router-dom": "^7.15.0"
  }
}
